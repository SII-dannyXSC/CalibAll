# 双臂默认配置：用户在 Web 端选两个 state_key，分别指定 start/stop
name: 默认（双臂）
dataset:
  _target_: caliball.dataset.lerobot_dataset.LeRobotDataset
  state_processor:
    _target_: caliball.dataset.state_processors.DualArmSliceProcessor
